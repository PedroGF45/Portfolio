# PedroGF45 — Portfolio (space-themed)

This repository is a single-page interactive portfolio built with React, Vite, TypeScript, Tailwind CSS and react-three-fiber.

Quick start

1. Install dependencies

```powershell
npm install
```

2. Run dev server

```powershell
npm run dev
```

3. Build

```powershell
npm run build
```

What I scaffolded

- Vite + React + TypeScript
- Tailwind CSS base
- A simple hero and a 3D solar-system scene using @react-three/fiber and @react-three/drei

Next steps

- Add ESLint/Prettier config
- Add more project data and images
- Add deploy workflow (Vercel or GitHub Pages)
